<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <EditText
        android:id="@+id/inputCurrency"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:padding="12dp"
        android:layout_margin="10dp"
        android:hint="##.##"
        android:inputType="numberDecimal"
        android:textSize="18sp"
        android:gravity="end"
        android:textColor="@color/black"
        android:background="@drawable/light_wight_round_bg"
        />

    <TextView
        android:id="@+id/selectedPrice"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@+id/inputCurrency"
        app:layout_constraintStart_toStartOf="@+id/inputCurrency"
        android:layout_marginTop="13dp"
        app:layout_constraintEnd_toStartOf="@+id/currencySpinner"
        android:layout_marginEnd="3dp"
        android:textSize="16sp"
        android:text=""
        android:textColor="@color/black"

        />

    <Spinner
        android:id="@+id/currencySpinner"
        android:layout_width="150dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:background="@drawable/spinner_bg"
        android:clipToPadding="true"
        android:dropDownSelector="@color/tv_rect_border"
        android:overlapAnchor="false"
        android:popupBackground="@drawable/spinner_row_rectangle_bg"
        android:spinnerMode="dropdown"
        android:textAllCaps="false"
        android:textColor="@color/black"
        android:textSize="18sp"
        app:layout_constraintEnd_toEndOf="@+id/inputCurrency"
        app:layout_constraintTop_toBottomOf="@+id/inputCurrency" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/currencySpinner"
        android:layout_marginVertical="15dp"
        android:layout_marginHorizontal="10dp"

        />

    <ProgressBar
        android:id="@+id/progressBar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:indeterminateTint="@android:color/holo_blue_bright"
        android:visibility="visible"
        android:elevation="10dp"/>




</androidx.constraintlayout.widget.ConstraintLayout>